# evolution_chamber/MAE/logging/kpis.py

def compute_kpis(scenario: str, seed: int) -> None:
    """
    Placeholder for KPI computation.

    Later: read per-agent CSVs and compute:
    - continuity metrics
    - message traffic stats
    - regime dwell times, etc.
    """
    print(f"[MAE] Finished scenario={scenario} seed={seed}")
