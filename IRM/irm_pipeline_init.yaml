status: experimental
role: "IRM micro-skeleton for mapping"
primitives: { state: S_t, time: t, identity: I, anchor: A }
operators: { T: "S_t->S_{t+1}", Ω: "S_t->O_t", β: "anchor update" }

stages:
  - intake      # normalize stimulus to internal tokens
  - update      # T, then Ω, then β
  - continuity  # snapshot/restore, light note only
  - emit        # produce a small outframe

outframe_min:
  fields: [t, anchors_touched, notes]

notes: "This file exists to name the path, not to implement it"
